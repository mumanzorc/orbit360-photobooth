'use client';import { useState } from 'react';
export default function EventForm({onClose,onSaved}:{onClose:()=>void;onSaved:()=>void}){const [saving,setSaving]=useState(false);async function submit(e:React.FormEvent<HTMLFormElement>){e.preventDefault();setSaving(true);const f=new FormData(e.currentTarget);await fetch('/api/events',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(Object.fromEntries(f))});onSaved();}return <div className="modal"><form className="panel" onSubmit={submit}><div className="panelHead"><div><span className="eyebrow">NUEVA EXPERIENCIA</span><h2>Configura tu evento</h2></div><button type="button" className="close" onClick={onClose}>×</button></div><div className="formGrid"><label>Nombre<input name="name" required placeholder="Gala Nova 2026"/></label><label>Cliente<input name="client" placeholder="Marca o anfitrión"/></label><label>Fecha<input type="date" name="date" required/></label><label>Lugar<input name="venue" placeholder="Santiago"/></label><label>Formato<select name="format"><option>9:16</option><option>1:1</option><option>16:9</option></select></label><label>Duración (seg.)<input type="number" name="duration" defaultValue="8" min="3" max="30"/></label><label className="wide">Mensaje de bienvenida<input name="welcome" defaultValue="¡Haz que este momento gire!"/></label><label>Color principal<input type="color" name="primary" defaultValue="#8b5cf6"/></label><label>Color acento<input type="color" name="accent" defaultValue="#f43f5e"/></label><label>Fondo<input type="color" name="background" defaultValue="#080a12"/></label><label className="wide">Marco (URL de PNG transparente)<input name="frameUrl" placeholder="https://.../marco.png"/></label></div><div className="panelActions"><button type="button" onClick={onClose}>Cancelar</button><button className="primary" disabled={saving}>{saving?'Creando…':'Crear evento'}</button></div></form></div>}
