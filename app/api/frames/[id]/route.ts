import {NextResponse} from 'next/server';import {currentUser} from '@/lib/auth';import {listFrames,removeFrame,saveFrame} from '@/lib/store';
export async function PATCH(req:Request,{params}:{params:Promise<{id:string}>}){if(!await currentUser())return NextResponse.json({error:'No autorizado'},{status:401});const {id}=await params,old=(await listFrames()).find(f=>f.id===id);if(!old)return NextResponse.json({error:'Marco no encontrado'},{status:404});return NextResponse.json(await saveFrame({...old,...await req.json(),id}))}
export async function DELETE(_:Request,{params}:{params:Promise<{id:string}>}){if(!await currentUser())return NextResponse.json({error:'No autorizado'},{status:401});const {id}=await params;if(id.startsWith('default-'))return NextResponse.json({error:'Los marcos incluidos no se eliminan'},{status:400});await removeFrame(id);return NextResponse.json({ok:true})}
